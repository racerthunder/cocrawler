Crawl:
  MaxCrawledUrls: 500 # prevent a runaway
  UserAgent: cocrawler-test/0.01
  MaxWorkers: 2
  PageTimeout: 1
  RetryTimeout: 0.1

Plugins:
  url_allowed: AllDomains

#Seeds:
  #Hosts:
  #- http://tut.by
  #Files:
  #- test-wide.hosts

Logging:
  Crawllog: ../log/crawllog.jsonl
  Robotslog: ../log/robotslog.jsonl
  Facetlog: ../log/facetlog.jsonl
  RejectedAddUrllog: ../log/rejectedaddurl.log

UserAgent:
  Style: crawler
  MyPrefix: test-wide
  URL: http://example.com/cocrawler.html
